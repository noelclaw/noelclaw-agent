{
  "_comment": "noelclaw configuration for Base chain — edit these values before starting",
  "api": {
    "_note": "Internal API config — no external API key required. All market data comes from free public sources."
  },
  "strategy": {
    "scanIntervalMs": 300000,
    "positionCheckMs": 10000,
    "maxOpenPositions": 3,
    "entryBudgetEth": 0.001,
    "minScanScore": 55,
    "minLiquidity": 50000,
    "stopLossPct": -6,
    "takeProfitPct": 25,
    "maxHoldMinutes": 45,
    "trailingStopActivatePct": 4,
    "trailingStopDistancePct": 3,
    "buyCooldownMinutes": 60,
    "_strategy_note": "entryBudgetEth: ETH per trade. buyCooldownMinutes: skip re-entry on the same token for this many minutes after closing."
  },
  "noelclaw": {
    "tokenAddress": "0x4B524015D54a27d4472F5c59c570730D69499Ba3",
    "_note": "NOELCLAW token on Base (ERC-20). The agent tracks and displays this balance."
  },
  "risk": {
    "maxEntry1hDropPct": -15,
    "blacklist": [],
    "safeOnly": false
  },
  "llm": {
    "model": "MiniMax-M2.7",
    "provider": "minimax",
    "baseUrl": "https://api.minimax.io/v1",
    "minimaxKey": "",
    "_note": "provider: 'minimax' (default) or 'ollama' (local). minimaxKey or MINIMAX_API_KEY env var. baseUrl overrides the provider default."
  },
  "survival": {
    "minEthWarning": 0.005,
    "minEthPause": 0.001,
    "noelclawReinvestPct": 0.25,
    "_note": "Warn when ETH < minEthWarning. Pause new buys when ETH < minEthPause. noelclawReinvestPct: fraction of each profit used to buy NOELCLAW (0.25 = 25%)."
  },
  "reflect": {
    "intervalMs": 14400000,
    "autoApply": true,
    "_note": "Self-improvement loop interval (default 4h). autoApply: true = auto-apply suggested config changes within safe bounds."
  },
  "telegram": {
    "token": "",
    "heartbeatChatId": null,
    "_note": "Set token here OR in TELEGRAM_BOT_TOKEN env var. heartbeatChatId = your Telegram user ID to receive heartbeat messages."
  },
  "heartbeat": {
    "intervalMs": 300000,
    "contextRefreshMs": 1800000,
    "_note": "intervalMs: proactive check-in cadence (default 5 min, 0 to disable). contextRefreshMs: how often to refresh cached ETH price + Fear & Greed index (default 30 min)."
  },
  "agentLoop": {
    "intervalMs": 5400000,
    "_note": "How often the agent reasons about and updates its session strategy (default 90 min). The LLM sets mode/patternFilter/minScore for the next window. Modes: active (auto-buy best scorer), selective (LLM gate per candidate), watchOnly (scan only, no buys)."
  }
}
